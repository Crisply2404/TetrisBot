# 轻量迭代：引擎仍回退 sim 的排错增强 + 校准锁定 + “自动对齐”按钮

目标：
- 让用户一眼看到“为啥没跑 cold-clear”（而不是只看到引擎=sim）。
- 用户校准过棋盘后，后续不要再自动裁剪/推断导致对齐漂移。
- 校准时提供一个“自动对齐”按钮：用户拖到大概位置后点一下，让框更接近标准网格/更好对齐。

任务清单：
- [√] 状态弹窗：即使没开调试模式，也显示 cold-clear 回退原因
- [√] 校准锁定：保存校准时自动开启 boundsLock，并记住校准时的 base bounds 模式
- [√] 校准吸附：校准工具栏新增「自动对齐」按钮（用 base bounds 做格子吸附 + 10x20 标准化）
- [√] offscreen/消息排错：把更具体的 offscreen 创建失败原因回传到 cold-clear 错误里
- [√] 验证：对改动 JS 做语法检查（node --check）

