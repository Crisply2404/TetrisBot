# 轻量迭代：提升对战“旋转优先”、窗口缩放对齐、默认完整块序

目标：
1) 对战模式下，尽量别“看起来只会消行”，如果 cold-clear 给了可用的 T-Spin/Spin 落点，优先选它；  
2) 用户缩放窗口后棋盘变小/变大，叠加层仍能跟着对齐；  
3) “读取完整块序”改为默认常开，不再给用户开关。

## 任务清单

- [√] 引擎：对战模式下从 cold-clear 的候选 moves 里优先挑可旋转落点（tspins/allspins）。
- [√] UI：弹窗/详情页显示 cc 的建议块/旋转/排行，方便判断“是不是根本没给旋转”。
- [√] 状态：兼容 `game.bag[0]` 偶尔包含当前块导致 next 错位。
- [√] 设置：readFullBag 默认常开，移除设置页开关；内部逻辑固定走 6→12→…→6。
- [√] 校准：记录校准时视口尺寸；窗口缩放后自动用相对比例重算 lockedRect。
- [√] 同步文档：更新知识库与 CHANGELOG。
- [√] 迁移方案包到 history 并更新索引。
